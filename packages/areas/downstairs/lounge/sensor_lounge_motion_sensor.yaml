template:
  - binary_sensor:
      - name: "Lounge - Motion Sensor Motion"
        device_class: motion
        state: >
          {% if states('sensor.lounge_camera_detection_fps') | int > 0  %}
            {{ 'on' }}
          {% else %}
            {{ 'off'}}
          {% endif %}